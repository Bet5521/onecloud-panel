# Build artifacts
dist/
web/dist/
*.exe
*.gz

# Runtime data (DB, logs, local test data)
runtime-data/

# Frontend deps
web/node_modules/

# IDE / tools
.trae/

# Logs & temp
*.log
*.tmp

# Runtime data dirs（含 secret.key / panel.db / 发布二进制 / 证书私钥，禁止入库）
runtime-data*/
runtime-agent*/

# 本地抓取物与临时脚本
cookies.txt
_ip.txt
_dltest
tmp_demo/
*.exe~

# 助手工作区（会话记忆 / 日志 / 报告底稿）。
# 里面会写真实内网地址、拓扑与排查细节（含现场口令片段），
# 仓库对外可见时一律不应入库。
.workbuddy/
