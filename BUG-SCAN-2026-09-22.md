# 全模块潜伏 Bug 扫描报告（2026-09-22）

## 结论

对 store / api / node / notify 全模块按 requirement#3 暴露的三类潜伏 Bug 模式做静态核查：
**未发现新的同类潜伏 Bug**。上轮修的 6 处（requirement#3 引入的通知模块薄弱点）是仅有的同类问题，已全部闭环。

代码库其余模块对「可 NULL 列扫进非空 string」「白名单枚举窄于实际使用」「用派生值静默覆盖传入值」三类模式均有正确防护，未见同类残留。

## 扫描方法

1. 读全部 `internal/store/migrations/*.sql`（001–013）确认每张表每列的 `NULL` 约束与 `DEFAULT`。
2. 读全部 store 层 `SELECT` 的 `Scan` 目标类型，对照结构体字段类型，判 NULL 扫描安全性。
3. 读全部 api 层请求校验（白名单 `map[string]bool`、`RequirePermission` 调用、`methodFromChannel` 类派生覆盖）。
4. 核对节点网络类型、角色权限点的枚举校验完整性。

## 三类模式与判定

| # | 模式 | 触发后果 | 本次结果 |
|---|------|----------|----------|
| 1 | 可 NULL 的 TEXT 列被 `Scan` 进非空 `string` | modernc 直接报 `converting NULL to string is unsupported` → 500 | 除已修 `notify_target` 外，无 |
| 2 | 白名单枚举（如 `validNotifyMethods`）窄于实际 `switch` | 合法值被拒 / 非法值静默放行 | 无残留 |
| 3 | 用派生值（如 `methodFromChannel`）覆盖客户端显式传入值 | 数据被静默篡改 | 无残留 |

## 逐模块核查结果

### 数据层（store）NULL 扫描安全性

| 表 / 列 | 可 NULL? | 扫描方式 | 结论 |
|---|---|---|---|
| `users.notify_target` | 是（011 ALTER 历史行） | `COALESCE(...,'')` | ✅ 上轮已修 |
| `users.notify_channel_id` | 是（009） | `**int64` 扫描（database/sql 原生支持 nil） | ✅ 安全 |
| `users.*`（real_name/phone/…） | 006/009 均 `NOT NULL DEFAULT ''` | 直扫 string | ✅ 安全 |
| `nodes.*`（hostname/os_name/kernel/arch/alt_address/network_type/address） | 001 建表可 NULL | 直扫 string | ✅ 实际不存 NULL（`CreateNode` 总以 Go 零值 `""` INSERT） |
| `nodes.owner_user_id` | 是（012） | `sql.NullInt64` 中转 | ✅ 安全 |
| `background_tasks.output/error/payload` | 001 可 NULL | 直扫 string | ✅ 实际不存 NULL（`CreateTask` INSERT 给 `''`） |
| `app_installations.service_name/container_id/container_name/params` | 001 可 NULL | 直扫 string | ✅ 实际不存 NULL（`CreateInstallation` 给零值） |
| `audit_logs.username/ip/detail/…` | 001 可 NULL | 直扫 string | ✅ 实际不存 NULL（`InsertAuditLog` 给全字段值） |
| `registration_tokens.description` | `NOT NULL DEFAULT ''` | 直扫 string | ✅ 安全 |
| `notification_channels.config_json` | `NOT NULL DEFAULT '{}'` | 直扫 string | ✅ 安全 |
| `notification_channels.tested_at` | 是（007） | `COALESCE(tested_at,0)` | ✅ 安全 |
| `custom_apps.owner_user_id/created_by` | 是（013） | `sql.NullInt64` 中转 | ✅ 安全 |

**关键判据**：modernc 仅在「列实际存 NULL 且扫进非空 string」时炸。除 `notify_target`（ALTER 历史行）外，所有可 NULL 列要么建表即存在且由 INSERT 给 `""` 零值（不存 NULL），要么 ALTER 带 `NOT NULL DEFAULT`，要么走 `NullInt64`/`**int64` 指针扫描。结论：store 层无新炸弹。

### 请求校验层（api）

| 校验点 | 位置 | 结论 |
|---|---|---|
| `validNotifyMethods` 枚举 | user_handlers.go:52 | ✅ 已修正为 `log/channel/email/sms`，与实际 `deliverToUserNotify` switch 一致 |
| `methodFromChannel` 覆盖式 | user_handlers.go:202/385/543 | ✅ 三处均为「空才推导、否则保留客户端值」兜底，无静默覆盖 |
| `notify.TargetFor(webhook).Needed` | notify.go | ✅ 已修正为 `false`（webhook 走通道固定 URL，不需每用户标识） |
| 节点 `network_type` 枚举 | ssh_node_handlers.go:124 | ✅ `node.IsValidNetworkType` 覆盖 lan/wireguard/public/local 全量 |
| 手动加节点 `network_type` | ManualAdd (node/service.go:233-238) | ✅ service 层空则推导、非空则校验，不依赖 handler |
| 角色权限点白名单 | role_handlers.go:84-99 | ✅ 基于 `store.AllPermissions`，拒绝未知/重复；Protected 角色不可改 |
| `RequirePermission` ↔ `store.PermXxx` | api.go 全量 | ✅ 每个 handler 均有对应权限枚举，无越权/缺权 |

## 防护机制总结（为何没炸）

- **建表约束**：可 NULL 的 TEXT 列几乎都带 `NOT NULL DEFAULT ''`，INSERT 不显式给值时存空串而非 NULL。
- **指针扫描**：所有 `*int64` 列统一用 `sql.NullInt64` 中转或 `**int64` 直扫，NULL 安全。
- **COALESCE**：`notify_target`、`tested_at` 两处历史 NULL 列已用 `COALESCE` 兜底（上轮修）。
- **白名单校验集中**：权限点、网络类型、通知方式均校验于单一权威函数/映射。

## 增强建议（非 Bug，按 P2 记录）

1. **P2 — `store.User.NotifyTarget` 类型诚实化**：当前用 `string` + `COALESCE(notify_target,'')` 兜底，功能正确但类型不诚实（列本可 NULL）。可升为 `*string` 去掉 COALESCE，需连带改 `reset_deliver.go` 两处 `TrimSpace` 与 DTO 输出（前端需兼容 `null`）。优先级低，当前方案零涟漪可保留。
2. **P2 — 自定义应用端到端真机验证**：上一轮仅做生产构建校验（`vite build` 通过），「上传二进制 → 安装 → 日志」链路未实跑。当前环境无运行中的三节点集群，建议具备真机后用 `SynthRecipe` + `PushCustomBinary` 路径实测。

## 验证状态

- `go build ./...`：EXIT=0（上轮）
- `go test ./...`：EXIT=0，全包通过（上轮修复回归后）
- 本次为静态代码审查，未改任何源码；结论可经上述测试复现。
